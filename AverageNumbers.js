// Write a basic function to calculate the average of two numbers.
function AverageNumbers(Num1, Num2) {
    return (Num1 + Num2) / 2;
}

var result = AverageNumbers(10, 20);
console.log(result);
